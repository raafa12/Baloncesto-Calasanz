---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`container mx-auto px-4 sm:px-24 md:px-48 lg:px-64 xl:px-80 pt-28 sm:pt-36 lg:pt-40 pb-20 sm:pb-28 lg:pb-32 max-w-7xl ${className || ''}`}>
  <slot />
</div>
