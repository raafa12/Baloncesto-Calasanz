---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`mb-6 sm:mb-8 w-full ${className} scroll-m-20 mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>