---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`mb-8 sm:mb-12 lg:mb-20 section ${className} scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>