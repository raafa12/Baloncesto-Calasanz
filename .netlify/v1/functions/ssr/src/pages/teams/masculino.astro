---
// src/pages/teams/masculino.astro
import Layout from '../../layouts/Layout.astro';
import SectionContainer from "../../components/common/SectionContainer.astro";
import PageContainer from "../../components/common/PageContainer.astro";
import TitleSection from "../../components/common/TitleSection.astro";
import MascIcon from "../../assets/Masc.astro";
import TeamSection from "../../components/teams/TeamSection.astro";

// Importar datos de equipos masculinos desde el archivo JSON
import equiposMasculinos from '../../data/equiposMasculinos.json';
---

<Layout title="Equipos Masculinos | Club Baloncesto Calasanz" description="Conoce a nuestros equipos masculinos en sus diferentes categorías">
  <PageContainer>
    <SectionContainer id="equipos-masculinos">
      <div class="flex items-center space-x-4 mb-8">
        <a href="/teams" class="text-green-400 hover:text-green-300 flex items-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 17l-5-5m0 0l5-5m-5 5h12" />
          </svg>
          Volver a Equipos
        </a>
      </div>
      
      <TitleSection>
        <MascIcon class="size-8" />
        Sección Masculina
      </TitleSection>
        
        <!-- Navegación rápida entre equipos -->
        <div class="flex flex-wrap justify-center gap-2 mb-10">
          {equiposMasculinos.map((equipo) => (
            <a 
              href={`#${equipo.nombre.toLowerCase().replace(/\s+/g, '-')}`} 
              class="px-4 py-2 bg-green-800 hover:bg-green-700 rounded-lg text-white text-sm font-medium transition-colors"
            >
              {equipo.nombre}
            </a>
          ))}
        </div>
        
        <!-- Equipos -->
        <div class="space-y-24">
          {equiposMasculinos.map((equipo) => (
            <div id={equipo.nombre.toLowerCase().replace(/\s+/g, '-')} class="scroll-mt-24">
              <TeamSection {...equipo} />
            </div>
          ))}
        </div>
      </div>
    </SectionContainer>
  </PageContainer>
</Layout>